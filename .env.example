DATABASE_URL=
# PostgreSQL / RDS: user must have CONNECT on the DB in this URL + privileges on schema public (?sslmode=require common on RDS).
# "User was denied access on the database" = grant CONNECT + table rights to that role in RDS/psql (not fixable in Node alone).
JWT_SECRET=
JWT_EXPIRES_IN=
PORT=
NODE_ENV=
# Optional: return underlying error message on 500 (local debug only; never public prod)
# EXPOSE_ERROR_DETAILS=true
FRONTEND_URL=



AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=


FLEX_BASE_URL=
FLEX_CLIENT_ID=
FLEX_CLIENT_SECRET=
FLEX_USER_CODE=
FLEX_RAW_PASSWORD=


ADMIN_API_KEY=