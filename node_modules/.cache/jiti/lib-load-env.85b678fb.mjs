"use strict";var _dotenv = _interopRequireDefault(await jitiImport("dotenv"));
var _path = _interopRequireDefault(await jitiImport("path"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}

/** Resolve `.env` from the backend package root (not `process.cwd()`), so `npm run dev` works even if cwd differs from `cbp-backend/`. */
const backendRoot = _path.default.resolve(__dirname, "../..");
const envFile = _path.default.join(backendRoot, ".env");

_dotenv.default.config({ path: envFile });
// Optional machine-specific overrides (same root)
_dotenv.default.config({ path: _path.default.join(backendRoot, ".env.local"), override: true }); /* v9-3a61052edcee4b0d */
