"use strict";var _path = _interopRequireDefault(await jitiImport("path"));
var _dotenv = _interopRequireDefault(await jitiImport("dotenv"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}

/**
 * Loads `.env.development` or `.env.production` based on NODE_ENV (defaults to development),
 * then `.env.local` with override for machine-specific secrets.
 * Must be imported before any code reads process.env (including Prisma).
 */
const root = process.cwd();
const nodeEnv = process.env.NODE_ENV || "development";
const primary =
nodeEnv === "production" ? ".env.production" : ".env.development";

_dotenv.default.config({ path: _path.default.join(root, primary) });
_dotenv.default.config({ path: _path.default.join(root, ".env.local"), override: true }); /* v9-bcce30e65e48aa84 */
